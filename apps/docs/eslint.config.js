import { react } from '@atom63/eslint-config/react'

// @atom63/design-system — Vite + TanStack Router app documenting the design system.
export default react({ tsconfigRootDir: import.meta.dirname })
