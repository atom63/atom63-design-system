import { ChangelogTimeline } from '../components/changelog-timeline'

# Changelog

Atom63 uses Changesets as its release source of truth. Pending changesets and generated package changelogs are aggregated here at build time, so this page reflects the monorepo without a second hand-maintained release log.

<ChangelogTimeline />
