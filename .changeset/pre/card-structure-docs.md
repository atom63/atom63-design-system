---
'@atom63/ui-react': patch
---

Document the Card structure. `CardHeader` is a single row for a `CardLabel` and a `CardAction`, while `CardTitle` and `CardDescription` go inside `CardContent`. This differs from shadcn, where the title sits in `CardHeader`.
