---
'@atom63/brand': patch
---

Add `@atom63/brand`: the ATOM63 logo components (`AppLogo`, `Atom63Logo`) and their path data, moved from atom63-vite. They need no stylesheet or Tailwind; the colored symbol uses `--a63-action-primary`, and a lockup is named once for assistive technology.
