# Vibe report, 2026-09-26

Generated by `pnpm vibe` (`scripts/design-system/vibe.mjs`); see `scripts/design-system/vibe/README.md` for how the runs are set up and how to read this report. The raw results are in `docs/design-system/audits/vibe/vibe-2026-09-26.json`.

- **Commit:** `b19811c8946b25a4ccc101b848a970edc7b781d1` (with uncommitted changes)
- **Model:** `claude-opus-5-5[1m]` (the `claude` CLI default)
- **Runtime:** `claude` 2.1.280 (Claude Code), headless (`claude -p`), at most 80 turns and $5.00 per builder, 30 turns and $2.00 for the judge
- **Briefs:** `sign-in`
- **Arms:** ds (Atom63), plain (React + Tailwind)

## Totals

| Measure | ds (Atom63) | plain (React + Tailwind) |
| --- | --- | --- |
| Briefs built | 1 | 1 |
| Craft violations | 0 | 44 |
| Type errors | 0 | 0 |
| Builds passing | 1 of 1 | 1 of 1 |
| axe violations (rules) | 0 | 0 |
| System-component share | 82% | 0% |
| Palette or arbitrary color utilities | 0 | 37 |
| Literal colors | 0 | 0 |
| Craft rubric (judged) | 21 of 21; 1 of 1 pass | 17 of 21; 0 of 1 pass |
| Builder cost | $0.60 | $0.17 |
| Builder time | 1m 16s | 42s |

A result passes the rubric when no criterion scores 1.

## Sign in (`sign-in`)

| Measure | ds (Atom63) | plain (React + Tailwind) |
| --- | --- | --- |
| Files | 1 added, 1 changed (240 lines) | 1 added, 1 changed (181 lines) |
| Craft violations (`check:craft` rules) | 0 | 44 |
| Type errors (`tsc --noEmit`) | 0 | 0 |
| `vite build` | pass | pass |
| axe violations | 0 rules, 0 nodes | 0 rules, 0 nodes |
| System-component share | 82% (23 of 28) | 0% (0 of 10) |
| JSX elements | 23 system, 2 third-party, 3 interactive HTML, 8 other HTML, 2 local, 1 icons | 0 system, 0 third-party, 10 interactive HTML, 17 other HTML, 1 local, 2 icons |
| Palette or arbitrary color utilities | 0 | 37 |
| Literal colors (hex, `rgb()`, `oklch()`) | 0 | 0 |

### Craft rubric

The judge saw ds (Atom63) as A and plain (React + Tailwind) as B. Scores are 1–3, with the judge's evidence.

| Criterion | ds (Atom63) | plain (React + Tailwind) |
| --- | --- | --- |
| Spacing rhythm | **3**: The page uses a small set of gaps: gap-1 inside the title block, FieldGroup's default gaps between fields, mb-6 between the header and the form, and gap-6 between the card and the… | **2**: Gaps come from the scale (space-y-4 between fields, mt-1 between a label and its input, mb-6 under the header, mt-6 before the footer prompt) and grouping is correct. However, spa… |
| Hierarchy | **3**: There is one variant="primary" full-width Sign in button. The CardTitle h1 'Tally' is larger and heavier than the CardDescription welcome line, and the footer prompt uses text-mut… | **3**: The single filled indigo Sign in button is the only primary action. The h1 at text-2xl font-semibold stands above the text-sm slate-600 welcome line, and the links step down to pl… |
| Alignment | **3**: Labels, inputs, the checkbox row and the button share the CardContent left edge. The Password label and the 'Forgot password?' link share a baseline through items-baseline justify… | **3**: Labels, inputs, the checkbox and the button all start on the card's p-8 edge. The Password label and the 'Forgot password?' link share a baseline through items-baseline, and both… |
| State completeness | **3**: The button uses loading={pending}, and a pending guard stops a second submit. The email field shows specific FieldError copy with data-invalid. A wrong password shows an error Ale… | **3**: While submitting, the button is disabled with aria-busy, shows a LoaderCircle spinner and changes its label to 'Signing in…'. The email field shows specific inline copy, the wrong… |
| Motion restraint | **3**: The page adds no custom animation. The only motion is the Button's built-in loading state, which comes from the component defaults. | **2**: The only motion is the loading spinner, which has a purpose. It uses Tailwind's animate-spin at the default duration instead of the --a63-motion tokens, and it has no motion-reduc… |
| Accessibility | **3**: Each input has a FieldLabel with htmlFor, and the email error is linked through aria-describedby with focus moved to the invalid field. The form error Alert has role="alert" and a… | **3**: The page uses native label/input pairs with htmlFor and a label that wraps the checkbox. It sets aria-invalid and aria-describedby on the email field, gives the form error role="a… |
| Token use | **3**: Styling comes only from components and role classes (text-primary, text-muted-foreground, text-sm, gap-6, max-w-sm). There are no literal colors, arbitrary lengths or className ov… | **1**: Colors are Tailwind palette literals throughout (bg-slate-50, bg-white, border-slate-300, bg-indigo-600, text-red-600, bg-emerald-50). The inputs, button and card are hand-rolled… |
| **Total** | **21 of 21**, pass (as A) | **17 of 21**, fail (as B) |

### Cost and time

| Run | Cost | Time | Turns | Stop | Denied tool calls |
| --- | --- | --- | --- | --- | --- |
| Builder, ds (Atom63) | $0.60 | 1m 16s | 28 | success | 1 |
| Builder, plain (React + Tailwind) | $0.17 | 42s | 7 | success | 0 |
| Judge | $0.20 | 29s | 9 | success | 0 |

Screenshots (JPEG thumbnails of the first screen): [sign-in-ds-desktop.jpg](vibe/2026-09-26/sign-in-ds-desktop.jpg), [sign-in-ds-mobile.jpg](vibe/2026-09-26/sign-in-ds-mobile.jpg), [sign-in-plain-desktop.jpg](vibe/2026-09-26/sign-in-plain-desktop.jpg), [sign-in-plain-mobile.jpg](vibe/2026-09-26/sign-in-plain-mobile.jpg).

## Briefs

Each builder got the brief inside this prompt, identical for both arms:

```text
Build the following as a new page of this app, reachable at the URL path `/<brief id>`. Keep the pages that already exist working.

## <brief title>

<brief body>

## Constraints

- Work only inside this project directory.
- Use only the dependencies that are already installed; do not add packages.
- Use realistic mock data where the page needs data; there is no backend.
- When you are done, `pnpm typecheck` and `pnpm build` must both pass.
```

### `sign-in`

```markdown
# Sign in

A sign-in page for a project management tool called Tally.

- A centered card with the product name, a short welcome line, an email field, a password field
  and a "Sign in" button.
- A "Forgot password?" link next to the password field and a "Create an account" link below the
  card.
- A "Keep me signed in" checkbox.
- Submitting with an empty or malformed email shows an error on that field. Any password other
  than `correct-horse` shows "Email or password is incorrect" above the button.
- While the request is in flight (fake it with a one-second delay), the button shows that it is
  working and cannot be pressed twice.
```
