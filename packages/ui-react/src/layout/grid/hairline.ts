/**
 * Shared hairline metrics for grid chrome rails / rules / sparkles.
 * Must stay in sync with Tailwind `border` (1px) on rail + rule strokes.
 */
export const GRID_HAIRLINE_PX = 1

/** Distance from the frame’s outer edge to the stroke’s optical center. */
export const GRID_HAIRLINE_CENTER_PX = GRID_HAIRLINE_PX / 2
