---
title: Installation
description: Run the docs locally and build them for production.
group: Getting started
order: 2
---

## Run locally

```bash
pnpm install
pnpm dev
```

## Build

```bash
pnpm build
```

The output goes to `dist/`. Serve it from any static host, and route unknown paths to
`index.html` so deep links work.
